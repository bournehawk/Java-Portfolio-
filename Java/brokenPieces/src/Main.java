/*  Name:   Sarah Hulcy
    Class:  MSITM 6340
    Date:   
 */

import java.util.Scanner;
import java.util.Random;

public class Main
{
    public static void main(String[] args)
    {
        String [] randomSentenceList =
                {
                        "Please wait outside of the house.",

                };

        // for(int i = 0; )
        //order to make a sent: a,n,v,adj,a,n

        // String randomSentenceGen = getRandomArticle() + " " + getRandomNoun() + " " + getRandomVerb() + " " + getRandomAdjective() + " " + getRandomArticle2().toLowerCase() + " " + getRandomNoun2();

        //System.out.print(randomSentenceGen);


    }
/*
    public static String getRandomArticle()
    {
        String[] article = {"The", "An", "A", "Some", "Any"};

        Random rand = new Random();
        int idx = rand.nextInt(article.length);
        String randArt = (article[idx]);
        //System.out.print(randArt);
        return randArt;
    }

    public static String getRandomArticle2()
    {
        String[] article = {"The", "An", "A", "Some", "Any"};

        Random rand = new Random();
        int idx = rand.nextInt(article.length);
        String randArt = (article[idx]);
        //System.out.print(randArt);
        return randArt;
    }

    public static String getRandomNoun()
    {
        String[] noun = {"city", "man", "dog", "woman", "goldfish"};

        Random rand = new Random();
        int idx = rand.nextInt(noun.length);
        String randArt = (noun[idx]);
        //System.out.print(randArt);
        return randArt;
    }

    public static String getRandomNoun2()
    {
        String[] noun = {"tree", "car", "computer", "mountain", "students"};

        Random rand = new Random();
        int idx = rand.nextInt(noun.length);
        String randArt = (noun[idx]);
        //System.out.print(randArt);
        return randArt;
    }

    public static String getRandomVerb()
    {
        String[] verb = {"grows", "slides", "poops", "jumps", "crawls"};

        Random rand = new Random();
        int idx = rand.nextInt(verb.length);
        String randArt = (verb[idx]);
        //System.out.print(randArt);
        return randArt;
    }

    public static String getRandomAdjective()
    {
        String[] adjective = {"bad", "slow", "hard", "quick", "clears"};

        Random rand = new Random();
        int idx = rand.nextInt(adjective.length);
        String randArt = (adjective[idx]);
        //System.out.print(randArt);
        return randArt;
    }
*/
}
//Scanner input = new Scanner(System.in);

    /*
    private static boolean hasGuessedWord(boolean[] correctlyGuessedLetters)

    private static boolean checkGuess(char guess, String words, boolean[] correctlyGuessedLetters)

    private static void printCurrentBoard(String word, boolean[] correctlyGuessedLetters, String hint)

     */

